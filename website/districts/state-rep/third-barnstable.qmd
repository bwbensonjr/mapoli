---
format:
  html:
    page-layout: full
execute:
  echo: false
  warning: false
params:
  office_name: "State Representative"
  district_name: "Third Barnstable"
title: Massachusetts `r params$district_name` `r params$office_name` District
---

```{r}
library(here)
source(here("website/_gen/legislative_info.R"))
```

```{r}
summary_text <- district_summary(params$office_name, params$district_name)
```

`r summary_text`

```{r}
dist_map <- district_map(params$office_name, params$district_name)
tmap_leaflet(dist_map)
```

<br/>

::: {.panel-tabset}

## Demographics

```{r}
district_demographics(params$office_name, params$district_name)
```

## Cities/Towns

```{r}
district_precincts(params$office_name, params$district_name)
```

:::

## Elections

```{r}
district_elections(params$office_name, params$district_name)
```


