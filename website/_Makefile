
generate_qmd_pages:
	uv run --with pyprojroot python _gen/generate_pages.py

render_html:
	find districts -name "*.qmd" -exec quarto render {} \;

fix_sitemap:
	perl -pi -e 's|<loc>([^<]*)/index\.html</loc>|<loc>$1/</loc>|g' ../docs/sitemap.xml

